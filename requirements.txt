# Core dependencies
ecdsa>=0.18.0
base58>=2.1.0
pynacl>=1.5.0
coincurve>=21.0.0
bip-utils>=2.9.0
mnemonic>=0.20
cryptography>=41.0.0
grpcio>=1.74.0
grpcio-tools>=1.74.0
protobuf>=6.31.0

# Web API dependencies
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic[dotenv]>=2.0.0
httpx>=0.24.0  # Required for FastAPI TestClient

# Testing and linting
pytest>=7.0.0  # For running the post-quantum test suite
flake8>=6.0.0
pytest-cov>=4.0.0

# Post-quantum cryptography dependencies
python-oqs>=1.0.0  # CRYSTALS-Kyber and CRYSTALS-Dilithium (requires Python 3.10+)

# Additional post-quantum dependencies
cryptography>=41.0.0  # For AES encryption of keys at rest

# Storage layer dependencies
python-rocksdb>=0.7.0  # RocksDB for ledger storage
neo4j>=5.0.0  # Neo4j driver for graph indexing
orbitdb>=0.1.0  # OrbitDB for IPFS-based proof storage
ipfshttpclient>=0.8.0  # IPFS HTTP client

# AI Verification Layer dependencies
opencv-python>=4.8.0  # Computer vision for face detection and activity recognition
face-recognition>=1.3.0  # Face recognition library
mediapipe>=0.10.0  # Pose detection and analysis
tensorflow>=2.13.0  # Deep learning framework for MobileNet
transformers>=4.30.0  # HuggingFace transformers for NLP
torch>=2.0.0  # PyTorch backend for transformers
sentence-transformers>=2.2.0  # Sentence embeddings for similarity analysis
scikit-learn>=1.3.0  # Machine learning utilities
speechrecognition>=3.10.0  # Speech recognition library
pyaudio>=0.2.11  # Audio recording and playback
pydub>=0.25.1  # Audio processing
aiohttp>=3.8.0  # Async HTTP client for blockchain integration

# Advanced Security & Token Features dependencies
py-ecc>=6.0.0  # BLS threshold signatures and elliptic curve cryptography
aioquic>=0.9.20  # QUIC transport layer for high-performance networking
dnspython>=2.3.0  # DNSSEC validation and DNS record management
cryptography>=41.0.0  # TLS certificate management and cryptographic operations

# AI Transparency & Accountability System dependencies
shap>=0.42.0  # SHapley Additive exPlanations for model explainability
lime>=0.2.0.1  # Local Interpretable Model-agnostic Explanations
numpy>=1.21.0  # Numerical computing for explainability calculations
pandas>=1.3.0  # Data manipulation for bias analysis
matplotlib>=3.5.0  # Plotting for transparency visualizations
seaborn>=0.11.0  # Statistical data visualization
jupyter>=1.0.0  # Interactive notebooks for AI research
ipython>=7.0.0  # Enhanced interactive Python shell
black>=22.0.0  # Code formatting
isort>=5.0.0  # Import sorting
mypy>=1.0.0  # Static type checking
bandit>=1.7.0  # Security linting
safety>=2.0.0  # Security vulnerability scanning
pre-commit>=3.0.0  # Git hooks for code quality
cassandra-driver>=3.29.2  # ScyllaDB/Cassandra driver

# Optional ZKP dependencies (research/experimental)
# Note: These are placeholder packages for research implementation
# Real ZKP integration would require: libsnark, circom, or similar
# zk-proofs>=0.1.0  # Zero-knowledge proof generation (research)
# circuit-compiler>=0.1.0  # Neural network to ZK circuit compilation (research)
